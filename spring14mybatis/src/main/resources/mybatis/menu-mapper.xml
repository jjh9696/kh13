<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="menu">
	
	<select id="list" resultType="MenuDto">
		select * from menu
	</select>
	
	<insert id="add">
		insert into menu (
			menu_no, menu_name_kor, menu_name_eng, menu_type, menu_price
		)
		values(menu_seq.nextval, #{menuNameKor}, #{menuNameEng}, #{menuType}, #{menuPrice})
	</insert>
	
	<update id="edit">
		update menu
		set menu_name_kor=#{menuNameKor}, menu_name_eng=#{menuNameEng}, 
		menu_type=#{menuType}, menu_price=#{menuPrice}
		where menu_no=#{menuNo}
	</update>
	
	<delete id="remove">
		delete menu
		where menu_no=#{menuNo}
	</delete>
	
	 <select id="find" resultType="menuDto">
	 	select * from menu where menu_no=#{menuNo}
	 </select>

</mapper>